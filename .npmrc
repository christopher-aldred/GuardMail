# Vite/Rollup/esbuild native binaries are installed for both darwin arches
# (see supportedArchitectures in pnpm-workspace.yaml) so the web build works
# whether node runs as arm64 or x86_64.
node-linker=hoisted
